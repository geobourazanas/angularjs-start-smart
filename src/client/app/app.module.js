(function () {
    'use strict';

    /**
    * Listing the main modules for the app
    * 
    * @file Listing the main modules for the app.
    * @namespace app
    */
    angular.module('app', [
        'app.core',
        'app.widgets',
        'app.products',
        'app.layout'
    ]);

})();